# yaml-language-server: $schema=./.dab-schema.json

bundle:
  name: de_associate_preparation
include:
  - resources/variables.yml
  - resources/pipelines/main.pipeline.yml
targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-a02cab6b-25f9.cloud.databricks.com
    variables:
      warehouse_id: 467889b6ae6e301f
      catalog: prep_dev
      schema: default
  prod:
    mode: production
    workspace:
      host: https://dbc-a02cab6b-25f9.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/mkovaliou@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/mkovaliou@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: 467889b6ae6e301f
      catalog: prep_prod
      schema: default
    permissions:
      - user_name: mkovaliou@gmail.com
        level: CAN_MANAGE
